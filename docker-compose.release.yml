services:
  easymail:
    image: vivibudong/easymail:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
      CORS_ORIGINS: http://localhost:3000,http://127.0.0.1:3000
    volumes:
      - ./data:/app/data
    ports:
      - "3000:8000"
